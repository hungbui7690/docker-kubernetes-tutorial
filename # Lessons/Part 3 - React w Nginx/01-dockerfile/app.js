/*
  Setup Docker React + Nginx
  1. create root/Dockerfile
  2. create .dockerignore
  

\\\\\\\\\\\\\\\\\\\\\\\\\

  - docker build -t react-nginx .
  - docker run -d -p 80:80 react-nginx

  🌲 multi stage dockerfile -> https://docs.docker.com/build/building/multi-stage/


*/
